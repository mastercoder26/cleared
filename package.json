{
  "name": "cleared",
  "private": true,
  "description": "An AI layer for Google Classroom, built for neurodivergent students.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev": "npm --prefix server run dev & npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "test": "npm --prefix server test",
    "typecheck": "npm --prefix web exec -- tsc --noEmit -p tsconfig.app.json",
    "lint": "npm --prefix web run lint"
  }
}
